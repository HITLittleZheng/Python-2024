"""
    if 嵌套
        if 条件表达式：
            满足条件表达式执行的代码块
            if 条件表达式2:
                满足条件表达式2执行的代码块
            else:
                不满足条件表达式2执行的代码块
        else:
            不满足条件表达式执行的代码块
"""

# 上地铁：口罩
# 门口：测温
mask = input("是否戴口罩('是'或'否')：")
temp = float(input("请输入体温："))

if mask == "是":
    print("上地铁...")
    # 检测体温 35.8 -- 36.9
    if 35.8 <= temp <= 36.9:
        print('进教室...')
    else:
        print('体温检测不通过, 回家')

else:
    print('回家......')
